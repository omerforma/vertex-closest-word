To run this server you can download the project and open it in eclipse.

When the server is running you can send an input as follow using curl:

curl -H "Content-Type: application/json" -d "{\"text\":\"c\"}" http://localhost:8080/api/words

c is the word you want as input.
